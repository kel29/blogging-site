<h1><%= @blog.title %></h1>
<h3><%= link_to @blog.user.name, users_path(@blog.user) %></h3>
<h6>
  <% @blog.tags.each do |tag| %> 
    <%= link_to "#" + tag.tag_name, tags_path(tag) %>
  <% end %>
</h6>
<p><%= @blog.content %></p>

❤️<%=@blog.likes%><%= link_to " like this post", like_path(@blog) %>
