
<div class="wrapper">
  <div class="container">
    <header class="blog-header py-3">
    </header>
      
    <!-- Most liked post -->
    <div class="jumbotron p-4 p-md-5 test-white rounded bd-dark">
      <div class="col-md-6 px-0">
        <h1 class="display-4 font-italic"><%= @featured.title %></h1>
        <p class="lead my-3">This will be the blog subtitle</p>
        <p class="lead mb-0">
          <p class="font-weight-bold"> <%= link_to "Continue reading...", @featured %></p>
        </p>
      </div>
    </div>

    
      <!-- Most recent posts -->
    <% @most_recent.each do |blog| %>
      <div class="row no-gutters border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
        <div class="col p-4 d-flex flex-column position-static">
          <strong class="d-inline-block mb-2 text-primary">#first #three #tags</strong>
          <h3 class="mb-1"><%= blog.title %></h3>
          <div class="mb-3 text-muted">MM/DD</div>
          <p class="card-text mb-2">This will be the blog subtitle</p>
          <p class="font-weight-bold mb-0"><%= link_to "Continue reading...", blog_path(blog)%></p>
        </div>
      </div>
    <% end %>
    
  </div>
  

  <main rols="main" class="container">
    <% if flash[:search_error] %>
      <div class="alert alert-danger" role="alert">
        <%= flash[:search_error] %>
      </div>
    <% end %>

    <%= form_tag blogs_path, method: 'get' do %>
      <%= label_tag :query, "Search through blog posts by tag:" %>
      <%= text_field_tag :query %>
      <%= submit_tag "search" %>
    <% end %>

    <ul>
      <% @rest_of_the_blogs.each do |blog| %>
        <li><%= link_to blog.title, blog %></li>
      <% end %>
    </ul>
  </main>

  <footer class="blog-footer" class="container-fluid">
    <p>Built by Krista</p>
  </footer>

  </div>
</div>
